server:
  port: 3344
  
spring: 
  application:
    name: springcloud-config
  cloud: 
    config: 
      server:
        git: 
          uri: git@github.com:qovd520/springcloud-config.git    # git 仓库的 ssh